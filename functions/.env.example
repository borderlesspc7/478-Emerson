# Copie para functions/.env (Firebase CLI carrega no emulator e deploy)
# NÃO coloque GOOGLE_APPLICATION_CREDENTIALS aqui — use functions/.env.local (só dev local).

PAGARME_SECRET_KEY=
PAGARME_WEBHOOK_USER=
PAGARME_WEBHOOK_PASSWORD=

STAYS_BASE_URL=https://bsc.stays.com.br/external/v1
STAYS_LOGIN=
STAYS_PASSWORD=
