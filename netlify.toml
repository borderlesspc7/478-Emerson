[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/stays/*"
  to = "/.netlify/functions/stays-proxy/:splat"
  status = 200

